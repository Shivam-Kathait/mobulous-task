export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN',
}

export enum ApprovalStatus {
    APPROVED = 'APPROVED',
    REJECT = 'REJECT',
    PENDING = 'PENDING',
}

export enum Status {
    BLOCK = 'BLOCK',
    ACTIVE = 'ACTIVE',
    DELETED = 'DELETED',
}

export enum OrderStatus {
    APPROVED = 'APPROVED',
    REJECT = 'REJECT',
}

export enum NotificationType {
    LOW_STOCK = 'LOW_STOCK',
}
