export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN',
}

export enum ApprovalStatus {
    APPROVED = 'APPROVED',
    REJECT = 'REJECT',
    PENDING = 'PENDING',
}

export enum Status {
    BLOCK = 'BLOCK',
    ACTIVE = 'ACTIVE',
    DELETED = 'DELETED',
}
